apiVersion: v1
kind: ConfigMap
metadata:
  name: app-configmap
  namespace: hospital-app
data:
  # Database configuration pointing to our internal cluster service
  DB_URL: "jdbc:postgresql://postgres-service:5432/hospital_db"
  DB_USER: "postgres"
  DB_NAME: "hospital_db"
